[package]
name = "agent-shell-displayserver-wayland"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "agent-shell Wayland display server protocol layer (pure wl_display core: connection, registry, global probe)"

[dependencies]
agent-shell-core.workspace = true
wayland-client = { version = "0.31", features = ["log"] }
